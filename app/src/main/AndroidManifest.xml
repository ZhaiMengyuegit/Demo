<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myreading">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/log"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" android:launchMode="singleTask"/>
        <activity android:name=".MainActivity" android:launchMode="singleTask" />
        <activity android:name=".PoemContentActivity" android:launchMode="singleTask"/>
        <activity android:name=".SentenceContentActivity" android:launchMode="singleTask"/>
        <activity android:name=".ArticleContentActivity" android:launchMode="singleTask"/>
        <activity android:name=".CollectActivity" android:launchMode="singleTask"/>
        <activity android:name=".EditUserActivity" android:launchMode="singleTask"/>
        <activity android:name=".BaseActivity" android:launchMode="singleTask"/>


    </application>
    <uses-permission
        android:name="android.permission.INTERNET"/>
    <uses-permission
        android:name="android.permission.GET_TASKS" />

</manifest>